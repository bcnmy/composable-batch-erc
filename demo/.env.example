# WalletConnect — get a free project ID at https://cloud.walletconnect.com
VITE_WC_PROJECT_ID=

# RPC URLs (server-side only — proxied via /api/rpc, never exposed to browser)
RPC_BASE=
RPC_ARBITRUM=
RPC_OPTIMISM=
RPC_POLYGON=
RPC_MAINNET=
